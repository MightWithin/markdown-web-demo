---
title: 模型指标汇总
---

# 模型指标汇总

这份速查表汇总机器学习、检索与大语言模型评估中常用的指标。评估时应同时关注**效果、稳健性、效率与安全性**，避免只用一个分数下结论。

## 1. 分类指标

设 TP、TN、FP、FN 分别为真正例、真反例、假正例、假反例。

| 指标 | 公式 / 含义 | 适用场景 |
|---|---|---|
| Accuracy | \((TP+TN)/N\) | 类别较均衡、误判代价相近 |
| Precision | \(TP/(TP+FP)\) | 控制误报，如垃圾邮件拦截 |
| Recall | \(TP/(TP+FN)\) | 控制漏报，如风险识别 |
| F1 | Precision 与 Recall 的调和平均 | 需要平衡误报与漏报 |
| ROC-AUC | 不同阈值下 TPR 与 FPR 的综合表现 | 比较二分类模型的排序能力 |
| PR-AUC | Precision–Recall 曲线下面积 | 正例稀少时通常更有解释力 |
| Log Loss | 对错误且自信的概率预测惩罚更大 | 评估概率质量 |
| MCC | 综合利用混淆矩阵四项 | 类别极不平衡时的单值指标 |

多分类任务常见聚合方式：

- **Macro**：先按类别计算，再等权平均，重视小类别。
- **Micro**：汇总所有样本后计算，受大类别影响更大。
- **Weighted**：按各类别样本数加权。

## 2. 回归指标

| 指标 | 特点 |
|---|---|
| MAE | 平均绝对误差，直观，对异常值相对稳健 |
| MSE | 平方误差，对大误差惩罚更强 |
| RMSE | MSE 开方，与目标变量同量纲 |
| \(R^2\) | 模型解释方差的比例；可能为负 |
| MAPE | 相对误差易理解，但真实值接近 0 时不稳定 |
| Huber Loss | 小误差用平方、大误差用绝对值，兼顾平滑与稳健 |

## 3. 文本生成与自然语言处理

| 指标 | 主要衡量内容 | 注意点 |
|---|---|---|
| Perplexity | 模型对文本序列的预测不确定性，越低通常越好 | 不同分词器之间不宜直接比较 |
| BLEU | 生成文本与参考文本的 n-gram 精确匹配 | 常用于机器翻译，对表达多样性不敏感 |
| ROUGE | 参考文本内容被覆盖的程度 | 常用于摘要 |
| METEOR / chrF | 词级或字符级匹配 | 对形态变化、字符级差异更友好 |
| BERTScore | 基于语义表示的相似度 | 依赖所选编码模型 |
| Exact Match | 预测与答案完全一致 | 适合问答、结构化输出 |
| 编辑距离 | 将预测变为目标所需的编辑操作数 | 适合 OCR、ASR、代码补全等 |

自动指标不能完全代替人工评审。开放式生成还应检查正确性、相关性、连贯性、风格和有害内容。

## 4. 检索与排序指标

| 指标 | 含义 |
|---|---|
| Precision@K | 前 K 个结果中相关结果的比例 |
| Recall@K | 所有相关结果中被前 K 个召回的比例 |
| Hit Rate@K | 前 K 个结果是否至少命中一个相关项 |
| MRR | 第一个相关结果排名倒数的平均值 |
| MAP | 各查询平均精确率的均值 |
| NDCG@K | 考虑相关性等级与排序位置的折损累计增益 |

RAG 系统通常同时评估：检索召回、上下文相关性、回答忠实度和最终任务成功率。

## 5. 大语言模型指标

### 任务效果

- **Accuracy / Exact Match / F1**：用于有标准答案的任务。
- **Pass@k**：k 次生成中至少一次通过测试的概率，常用于代码生成。
- **Win Rate**：成对比较中模型获胜的比例。
- **校准误差**：模型置信度是否与实际正确率一致。

### 生成质量

- **事实性与忠实度**：答案是否由事实或给定上下文支持。
- **指令遵循**：是否满足格式、约束与用户意图。
- **相关性与完整性**：是否答到重点并覆盖必要信息。
- **一致性**：多次运行或改写提示后是否保持稳定。

### 安全与责任

- 有害内容率、越狱成功率、隐私泄露率。
- 不同人群和语言上的性能差异。
- 拒答准确率：该拒绝时拒绝，不该拒绝时正常回答。

### 系统效率

| 指标 | 含义 |
|---|---|
| TTFT | 首个 token 返回时间 |
| Tokens/s | 稳态生成速度 |
| 端到端延迟 | 从请求到完整响应的总时间 |
| 吞吐量 | 单位时间可处理的请求或 token 数 |
| 单次成本 | 每次请求、每千 token 或每个成功任务的成本 |
| 上下文利用率 | 长上下文中有效信息被正确使用的程度 |

## 6. 如何选择指标

1. 先定义真实业务目标与错误代价。
2. 选择一个主指标，并搭配若干护栏指标。
3. 按类别、场景、语言和难度分组报告。
4. 同时给出均值、分布、置信区间和样本量。
5. 固定数据集、提示模板、解码参数与评测版本。
6. 自动评测与盲测人工评审交叉验证。
7. 上线后持续监控漂移、延迟、成本与安全事件。

> 一个模型“更好”，应意味着它在目标场景中以可接受的成本，更稳定、安全地完成任务，而不只是某个排行榜分数更高。
